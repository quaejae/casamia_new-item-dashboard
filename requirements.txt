# 대시보드 실행에 필요한 패키지 — 검증된 버전으로 정확히 고정(==)
# 이유: Streamlit Cloud는 재빌드할 때마다 패키지를 새로 설치한다.
#       범위(>=)로 두면 그때그때 최신 버전이 깔려 "어제 되던 게 오늘 깨지는" 일이 생긴다.
#       아래 버전은 로컬에서 정상 동작을 확인한 조합이다.
# 버전 업그레이드 방법: 로컬에서 새 버전 테스트 → 정상 확인 후 아래 숫자만 교체 → 커밋/푸시.
streamlit==1.58.0
openpyxl==3.1.5
python-pptx==1.0.2
pandas==2.3.3
Pillow==12.2.0

# Google Sheets 비공개(서비스계정) 모드용. 현재 public 모드에선 미사용이나
# 대체 경로(sheets_loader.py)를 위해 버전 고정해 보존.
gspread==6.2.1
google-auth==2.53.0
